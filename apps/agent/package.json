{
  "name": "@centinela/agent",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@centinela/contracts": "workspace:*",
    "@centinela/croma": "workspace:*",
    "agents": "^0.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.123.0"
  }
}
